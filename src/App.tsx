// PR1 placeholder shell — the endfield design system lands in a later PR.
export default function App() {
  return (
    <main style={{ minHeight: "100svh", display: "grid", placeItems: "center" }}>
      <h1>FIELD VAULT · 场站验证库</h1>
      <p>工程骨架验证页 / scaffold check</p>
    </main>
  );
}
